// auth.js - small niceties for login/signup pages
document.addEventListener('DOMContentLoaded', function(){
  // auto-focus first input
  const first = document.querySelector('.login-box input');
  if(first) first.focus();
});
